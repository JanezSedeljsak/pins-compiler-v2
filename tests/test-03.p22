typ x = char ;
var y : arr ;
var s : int;

#{
    Metoda prejme ful kul parametre
    #{ Vrne pa nothing }#
}#
fun Metoda (a: int, b: char, c: arr) : void = {
    y^ = get_arr_pointer();
    x = _get_char(55, 'a', 8, 9, 10);
    s=(5+4 : int);
    a^=(5+4 where typ x = int; var z : arr;);
    
    if y[0] == 5 | !(x == (4 * 5)) then
        c('4');
    end;
    
    if y[(2*3/3)] <= 6 then
        s = Add_(5,4);
        g();
    else
        s = (5 + 8) * 8;
    end;

    while s^ > 0 | y[0] == 5 | (y[1] > 3 | a != 2) do
        print(s);
        s^ = s^ - 1;
    end;

    if ((b != 3) | (x & !b)) then
        a();
    end;
};

fun Add_(a: int, b: int) : int = {
    result = a + b;
    result;
};